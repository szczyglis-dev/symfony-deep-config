{% extends 'base.html.twig' %}

{% block title %}DeepConfig{% endblock %}

{% block body %}
    <h1>DeepConfig</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deep_config.id }}</td>
            </tr>
            <tr>
                <th>Param</th>
                <td>{{ deep_config.param }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ deep_config.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('deep_config_index') }}">back to list</a>

    <a href="{{ path('deep_config_edit', {'id': deep_config.id}) }}">edit</a>

    {{ include('deep_config/_delete_form.html.twig') }}
{% endblock %}
